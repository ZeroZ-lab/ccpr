# Domain Docs

This repository uses a single domain context.

Before working in the codebase, read:

- `CONTEXT.md` for canonical domain language
- relevant ADRs under `docs/adr/`

Use glossary terms in issues, tests, and implementation. Surface any conflict with an ADR rather than silently overriding it.
